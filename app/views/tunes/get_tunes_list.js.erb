$('#tune_list').hide();
$('#tune_list').empty();

$('#tune_num').hide();
$('#tune_num').empty();

$('#tune_list').html(
    "<%= escape_javascript(render :partial => 'tunes_list_body') %>"
    );
$('#tune_list').fadeIn();

$(".progress").each(function(elem){
  $(this).progressBar(progress_default_option);
});

$('#tune_num').html("<%= @tunes.count %>")
$('#tune_num').fadeIn();

