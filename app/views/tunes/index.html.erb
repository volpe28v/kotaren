<div id="menu">
<%= form_tag( url_for(:action => 'get_tunes_list'), :remote => true ) do %>
  <%= select_tag('album_title', options_for_select(["All Albums"] + @albums.map(&:title)) ) %></br>
    <%= select_tag('tuning_name', options_for_select(["-"] + Tuning.all.map(&:name).uniq.sort) ) %>
    <%= submit_tag('Update') %>
  <% end %>

  <br><br>
  <div><center><span id="tune_num">0</span> tunes.</center></div>

</div>

<div id="main">
  <div id="tune_list"></div>
</div>
